name: bahu_erp_mobile
description: Unified ERP Flutter app shell
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.6.0

flutter:
  uses-material-design: true
